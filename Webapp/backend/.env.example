# Security
# The secret key for JWT authentication (Use a secure random string in production)
# You can generate one via: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=ReplaceThisWithSecureKey

# Configuration
# The pixel-to-millimeter calibration ratio for disk analysis
PIXELS_PER_MM=10.0
